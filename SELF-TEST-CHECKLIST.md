# Phase 1 自测清单

对应 [ROADMAP.md](./ROADMAP.md) 的 Phase 1。目的：开发者自己（以及 1-2 个身边人）先用一遍，抓明显的 bug 和体验问题。

## 数据纪律（先读，违反即事故）

- 自测数据**不计入正式验证样本**（[CLAUDE.md](./CLAUDE.md) 规则 3），避免重演 ECOS 里"lbc001=开发者本人"被当正式样本的问题
- 自测一律用独立 `--user selftest_*` + 独立 `--db`，不污染正式 `data/cogmirror.db`
- 测试者 ≠ 开发者：凡是开发者自己测出来的数据，记录时标注"自测数据"

## A. 关卡问题（唯一硬门槛，不过不进 Phase 2）

> 第一次见到它的人，能否在**没有开发者在场解释**的情况下，看懂"认知地图"在说什么？

| # | 检查项 | 通过条件 | 状态（开发者视角初步判断，正式结论待 Phase 2 真实用户） |
|---|---|---|---|
| A1 | 5 个维度标签自明 | 外行能凭标签+数值说出"我哪强哪弱"，不靠猜 | 🟡 初判可懂：标签带括号解释；X/C 诚实标注（F 表第 5、11 行已修可读性） |
| A2 | Bloom 六层自明 | 看得懂"主导层级"；L5/L6 的"暂无题目"标注不引起困惑 | 🟡 初判可懂：主导层级不再误判（F 表第 7 行）；L5/L6 标注"暂无题目" |
| A3 | 伪自信点可理解 | 懂"自评高但实际差"的含义，且不觉得被冒犯 | 🟡 初判可懂：措辞"『感觉会』可能掩盖了『其实还没会』"偏中性；"被冒犯感"需真人验证 |
| A4 | 临界概念可理解 | "这不是退步，是正常中间态"的安抚有效，且进度数值能看懂 | ✅ 初判可懂（2026-08-27）：进度不只百分比，还翻译成行动语言「再答对 N 次 L3+ 题即跨越」（F14 行真机验证）；liminal 与「已跨越」三态端到端已可达（F10 收口）；「安抚措辞是否有效」仍待 Phase 2 真人评估 |
| A5 | 一句话建议可执行 | 具体到"下一个该做哪个 topic / 哪个层级 / 几道题" | ✅ 真机可执行（2026-08-27）：地图末尾直接问「按建议现在练吗？[y/N]」，答 y 即进入目标题组练习轮（F13 行）；建议带"为什么"——正在跨越/当前最弱 + 剩余次数（F14 行）；具体性待 Phase 2 真人评估 |

## B. 链路完整性（工程线）

| # | 检查项 | 通过条件 | 状态 |
|---|---|---|---|
| B1 | 首次进入→答题→出图 | 全程无崩溃、无 traceback | ✅ 脚本 + 真机（弱学习者 23 题、全对 10 题、真机 3 题均无崩溃） |
| B2 | 二次进入恢复历史 | 提示"已完成 N 次作答"；`--map-only` 可用 | ✅ `test_map_only_and_restore` 过；真机同一 user 跑两次，第二次恢复并累积（F 表第 11 行） |
| B3 | 5D 数值合理 | 非全零、非全同；K/P/S 有区分；C 随伪自信下降；X 诚实标注 | ✅ `test_5d_not_degenerate_after_answers` 过；真机 K=0.73/P=0.52/S=0.56 有区分；弱学习者 C 随 9 处命中下降 |
| B4 | 数据落库 | `responses` / `belief_snapshots` 有记录，user_id 正确 | ✅ 直接查 /tmp/cogmirror_real.db：6 条 responses + belief_snapshots，user_id=selftest_real |

## C. 判分正确性

| # | 检查项 | 通过条件 | 状态 |
|---|---|---|---|
| C1 | choice 对/错 | 对 1.0、错 0.0，选项解析正确 | ✅ 真机 Q1 选 1→1.00、Q2 选 2→1.00；单测覆盖错误选项 0.0 |
| C2 | fill 精确匹配 | 大小写/空格归一化；可接受写法命中 | ✅ 真机 Q3 `[1, 2, 3]`→1.00；归一化单测覆盖 |
| C3 | code 部分正确 | 部分用例通过 → 部分分（partial credit 真实产生） | ✅ 弱学习者场景真实产生：sum_to 0.33、max_of 0.67、count_vowels 0.50 |
| C4 | code 异常容错 | 语法错/超时/未定义函数 → 友好提示，不崩溃 | ✅ 弱学习者 pr-l3-01（无限递归）友好显示「异常: maximum recursion depth exceeded」，不崩溃 |
| C5 | 闭包题专用判分 | `make_counter` 返回值是函数，走专用校验 | ✅ 弱学习者 ps-l3-01 走专用路径（得部分分）；正确闭包回归测试过 |

## D. 认知信号正确性

| # | 检查项 | 通过条件 | 状态 |
|---|---|---|---|
| D1 | 伪自信判定 | 自评高+答错 → 命中；自评低 → 不命中 | ✅ 弱学习者 9 处命中；真机校准用户「未发现失准（5 次自评与表现一致）」不误报 |
| D2 | 临界概念三态机 | L3+ 正确推进 progress；liminal→post 需连续 3 次；答错重置连续计数 | ✅ 已验：detector 单测 + 引擎集成测试双线通过；端到端 CLI 已验证 liminal 进入并渲染「跨越进度 90%」。已知限制见 F 表第 10 行（post_liminal 产品内暂不可达） |
| D3 | Bloom 分布随表现动 | 得分涨/落，对应层级概率跟随 | ✅ `test_bloom_update_responds_to_score` 过；真机全对 L2=0.98 vs 未练 L3/L4=0.50 拉开 |
| D4 | C 维度非死 | 伪自信/misconception 命中后 C 下降，且不被 MIRT 重算抹掉 | ✅ `test_illusory_hit_discounts_c_mastery` 过（discount_factor 持久化，见 CHANGELOG） |

## E. 体验（真人主观，脚本验证不了的必须真机复核）

| # | 检查项 | 通过条件 | 状态 |
|---|---|---|---|
| E1 | 自评 prompt 与选项/输入提示 | 顺序、换行清晰不粘连（脚本下会粘连，需真机确认） | ✅ 真机通过（2026-08-22）：跳过自评后「输入选项编号:」另起一行不粘连 |
| E2 | 非法输入容错 | 非整数自评、非法选项编号 → 友好重试不死循环 | ✅ 真机通过（2026-08-22）：`abc`/`150` 均提示重试且不循环；自评部分无死循环 |
| E3 | 代码输入结束标识 | "单独一行输入 END" 明确可达 | ⏳ 脚本已覆盖（弱学习者会话用 END 正常结束判分）；真机待补（这轮 3 题无代码题） |
| E4 | 输出密度 | 不冗长、不密到看不清 | ✅ 真机通过：3 题输出 + 认知地图密度可读 |

## F. 记录表

| # | 发现 | 严重度(高/中/低) | 触发场景 | 处理(修/记SOMEDAY/待复核) |
|---|---|---|---|---|
| 1 | 递归代码被判 NameError（正确答案 0.67 部分分） | 高 | pr-l3-01 提交正确递归 `factorial` | 修：`grade_code`/`_grade_make_counter` 的 exec 改同一 dict；已加回归测试 |
| 2 | choice 无效选项：显示"无效选项"+得分 0，不重答 | 低 | 选项输入 "9" / 非数字 | 记 SOMEDAY：MVP 可接受，后续可做重答 |
| 3 | 自评 prompt 无换行：跳过自评时与下一提示粘连（真机复现，2026-08-22） | 低 | 直接回车跳过自评 | 修：`ask_self_confidence` 的 input prompt 末尾加 `\n` |
| 4 | 一句话建议仍建议"刚做完的 topic" | 低 | 连续练同 topic 后 | 复核后判定：逻辑正确（variables 进 liminal 后未凑够 3 次 L3+），观感需真新人评估 |
| 5 | C 置信度无命中时停在 0.55，被读成"中等自信"（实为校准信息不足） | 中 | 全对 + 无伪自信场景 | 修：C 改为状态显示——无失准证据时显示"未发现失准（N 次自评与表现一致）"，不再给先验数值 |
| 6 | Bloom 六层数值差异不大（步长 0.05，全对也挤在 0.5-0.65） | 中 | 全对场景 | 修：bloom_update_step 0.05 → 0.12，练过/未练层拉开差距 |
| 7 | 弱学习者显示「主导层级 EVALUATE」：只练 L1-L4 且全错时，未练的 L5/L6 停先验 0.5 反超 | 高 | 全错弱学习者 | 修：`covered_layers` 只从有观测的层里选主导，随序列化持久；已加回归测试（engine + state） |
| 8 | 伪自信计数与列表不一致：C 说「发现 8 处失准」但只列 5 条（`[-5:]` 截断） | 中 | 8+ 处伪自信命中 | 修：全量列出，已加 6 命中端到端测试 |
| 9 | 一句话建议露出原始英文 id「python.recursion」，新手看不懂 | 中 | 最弱 topic 为英文 id | 修：TOPIC_LABELS 中文短名，已加端到端 + 单元测试 |
| 10 | 临界概念可观测性受限：仅 loops 有 3 道 L3+（可进 liminal），其余 topic 只有 2 道（永远进不了 liminal）；无 topic 能凑满 6 次 L3+ 到 post_liminal，「已跨越」态产品内不可达。规则本身已验证（detector+引擎双线） | 低 | 任意单 topic 全对 | ✅ 修（2026-08-26）：5 个 topic 各补到 6 道 L3+（+18 题，题库 20→41）；认知地图补「已跨越」确认文案；CLI 加 --topic/--level 让建议可执行（见 F 表第 12 行真机验证） |
| 11 | C「未发现失准（N 次自评与表现一致）」的 N 是跨会话累积：单次会话只填 2 次自评却显示 5（前一轮会话有 3 次），初看会以为数错 | 低 | 多次会话共用一用户 | 复核后判定：非 bug（累积校准证据语义）；观感是否要改（如改为"近 N 次"）等 Phase 2 真实用户反馈再定 |
| 12 | 真机验证（2026-08-26，自测数据，不计入正式验证样本）：`cogmirror --topic python.loops --level L3 --questions 3` 全对 → 地图见「正在跨越中 循环（跨越进度 90%）」+「立刻练习」命令；再跑同命令 3 题全对 → 「已跨越（恭喜）循环」；`--map-only` 恢复确认持久。边界：`--topic python.recursion --level L6`（无题）提示友好不崩溃 | — | 真机 CLI | ✅ 验证通过（pytest 203 全绿 + 真机双指标） |
| 13 | 交互式「按建议直达练习」真机验证（2026-08-27，自测数据，不计入正式验证样本）：主会话 3 道 loops-L3 全对 → liminal 90% + 地图末尾交互提示「按建议现在练 3 道「循环」的 L3 题吗？[y/N]」；答 y → 进入练习轮 3 题全对 → 重渲染地图「已跨越 循环」且不再二次提示（已跨越不再建议）；答 n → 拒绝后只渲染一次地图即退出，不出题不循环 | — | 真机 CLI（主/练习轮双分支均覆盖） | ✅ 验证通过（pytest 205 全绿 + 真机双分支） |
| 14 | 建议进度文案真机验证（2026-08-27，自测数据，不计入正式验证样本）：主轮 3 道 loops-L3 全对 → 地图 liminal 行「循环是受控的重复（跨越进度 90%，再答对 3 次 L3+ 题即跨越）」+ 建议「「循环」正在跨越中——再答对 3 次 L3+ 题即跨越」+ 练习提示「（再答对 3 次 L3+ 题即跨越）[y/N]」；答 y 练习轮 3 对 → 「已跨越」。另一用户全答错 → 建议「「变量赋值」是当前最弱的一项（掌握概率 11%）」，练习提示无剩余次数（非 liminal 目标不加） | — | 真机 CLI（liminal/最弱两分支） | ✅ 验证通过（pytest 208 全绿 + 真机双分支） |

| 15 | P1 黄金回归证伪验证（2026-08-28，自测数据，不计入正式验证样本）：真实文件改 `ILLUSORY_GAP_THRESHOLD` 0.5->0.4 -> `pytest -m regression` 报 `REGRESSION: illusory_hits: 1 -> 2`（首版序列曾漏报该改动--所有 gap 都 ≥0.5 使改动不可观测，补 calibration_boundary_learner 边界序列后抓到）；还原后全绿。另 3 个 seeded 证伪测试（折扣幅度/落差阈值/自评下限）已自动化固化 | - | 回归基建验收（证伪测试） | ✅ 验证通过（回归抓到 seeded 破坏，P1 决策关卡通过） |
| 16 | P2 校准曲线真机验证（2026-08-31，自测数据，不计入正式验证样本）：①真实历史（local_user，10 次作答/9 次自评）`--map-only` -> 地图新增「自评校准度（ECE）: 0.08」与 C 行「未发现失准（9 次自评与表现一致）」自洽；②临时用户 selftest：首轮 5 题"自评 90 全错"（固定折扣，C 21%），次轮恢复后再答 1 题自评 90 答错 -> 曲线驱动折扣夹到 0.5，C 21% -> 10%（「与上次相比 C 置信度: -11%」）、ECE 0.81 正确反映严重失准、当题伪自信提示正常 | - | 真机 CLI（恢复注入/曲线驱动折扣两分支） | ✅ 验证通过（pytest 294 全绿 + 黄金回归基线零漂移 + 真机两分支） |

---

## 走一遍脚本（可复用，覆盖关键边界）

覆盖：部分分代码题、语法错误、fill 答错、伪自信命中/不命中、闭包题、错误输入、二次进入恢复。用独立 user/db。

```bash
python -m cogmirror.cli --user selftest_walk --db /tmp/cogmirror_selftest2.db --questions N
```

自测记录在 `docs/` 外单独保存，或在 F 表里打勾。数据标注：`自测数据，不计入正式验证样本`。
