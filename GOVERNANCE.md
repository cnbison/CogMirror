# GOVERNANCE：治理硬规则

> **性质**：这些规则不是建议，是硬约束。写给未来的自己，也写给协助开发的AI助手（如果用Claude Code等工具协作，建议把本文件内容同步进项目的AI协作说明文件，如`CLAUDE.md`）。
> **来源**：全部脱胎于ECOS项目两个多月的真实教训，不是凭空制定的流程主义。

---

## 规则1：不虚标完成度

Commit信息、文档、README里的"完成""✅"标记，必须对应**真实可验证的行为**，不能对应"代码写完了但没跑通/没测过"。ECOS项目历史上因为这个问题累计发生过4次"虚标"（比如某功能被标记为"完整产品形态"，但核心指标从未真正非零过）。

**具体执行**：任何标记为"完成"的功能，标记前必须能回答"如果现在有人问我要一个可复现的演示，我能不能立刻做到"。答不了，就不能标完成。

## 规则2：任何"验证通过"的结论，需要至少两种独立方法交叉印证

单一指标/单一测试跑通，不构成"验证通过"。ECOS的H3假设两次栽在这个问题上：第一次的"通过"是重新定义指标之后才通过的；一次"完美结果"（0拐点）后来被发现是三个bug叠加的假象，而不是真的表现完美。

**具体执行**：在文档里发布"某功能/某假设已验证"之前，问自己——如果换一种测量方式，结论还成立吗？如果答不上来，先标注为"初步观察，待复核"，不要标"验证通过"。

## 规则3：测试者不能是开发者本人

ECOS阶段"3个测试用户"里第一个（lbc001）就是开发者自己，这严重削弱了"真实用户验证"这个说法的含金量，且这一点直到复盘阶段才被明确指出。

**具体执行**：任何写入正式验证记录（尤其是要用来做产品/战略决策的验证）的用户数据，必须来自非团队成员。开发者自测的数据可以用于调试，但要在文档里明确标注"自测数据，不计入正式验证样本"。

## 规则4：抽象化/通用化工作需要日历硬约束

ECOS在6月27日已经自我诊断出"框架先于应用"是最大风险，但8月11-17日（v0.83-v0.94）仍然投入了一周做Kernel通用化，直到8月17日才第二次做方向审查、拍板转向"验证优先"。**知道问题存在，不等于会自动避免重犯**，需要外部约束而不是依赖自觉。

**具体执行**：任何"要不要把某个具体能力做成可插拔的通用模块"的讨论，默认延后到——
1. 至少有一轮真实用户验证结果支撑该能力值得抽象
2. 或者已经出现第二个具体场景需要复用该能力（而不是"预计将来会需要"）

在此之前，遇到"这里可以做得更通用一点"的冲动，写进一个`SOMEDAY.md`文件里记下来，然后继续手头的具体验证工作，不要立刻动手做。

## 规则5：版本号与完成度标注要对应真实验证阶段，不是工程量

版本号增长速度不应该被当作进度的代理指标。一周内跳好几个小版本、大量"100%完成"标记堆积，是ECOS项目里"工程产出量"和"科学验证进度"这两条线脱节的典型信号。

**具体执行**：在阶段性文档（README、Roadmap）里，明确用两条独立的进度线分别汇报："工程完成了什么"和"验证证实了什么"，不要合并成一条笼统的"进度"。

## 规则6：数据事故必须记录污染范围，不能只当bug修完就结束

ECOS在2026年8月19日发现一个持久化bug，导致某测试用户三周的真实数据丢失/错误写入，而这三周恰好覆盖了多轮重要的算法验证周期。这个bug被修复了，但"这三周的数据丢失，污染了哪些此前发布的验证结论"这个问题在发现时没有被同步回答。

**具体执行**：任何影响到已使用数据得出结论的数据事故（丢失、错误写入、串号等），修复bug之后必须追加一步——列出所有依赖受影响时间窗口数据的既有结论，逐条标注"是否需要用新数据重新验证"。

---

## 关于本文件的维护

本文件不是一次性写完就不再变的。如果新项目又踩到了新的坑，按ECOS的先例（每次踩坑后写`discussions/`记录、提炼成硬规则），把新教训加进来，保持这份文件是"活的"经验总结，而不是摆设。
