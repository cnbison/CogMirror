# SOMEDAY：暂缓事项清单

> **用途**：按[GOVERNANCE.md](./GOVERNANCE.md)规则4执行——任何"要不要做得更通用一点"的冲动，先记在这里，不要立刻动手做。触发条件写清楚，条件不满足之前不重新讨论。

---

## 1. 跨领域认知诊断（不局限于Python）

**想法**：认知水平和结构不应该局限于某一个专业技能，理论上对话式诊断可以跨领域评估用户的整体认知状态。

**为什么现在不做**：
- Phase 0.5 spike的实验设计需要固定"技能图谱是否可靠"这个变量，只测"对话能不能提取出可信信号"。如果同时把领域也放开，诊断结果不准时无法判断是方法论问题、跨领域认知模型假设问题，还是具体领域缺乏可用图谱——三个未知变量、一组结果，实验作废。
- "跨领域测一个人整体认知水平"在心理测量学里对应的是"一般认知能力/智力测量"方向（g因子、流体/晶体智力理论），是比"测单一学科知识掌握度"验证难度高得多、争议大得多的领域。
- 容易让用户把产品理解成某种智力/能力测评，与PRD里"这是学习状态画像，不是心理测评"的边界产生张力，需要格外谨慎的产品语言设计，不是单纯的技术选型问题。

**触发条件**：Python这一个领域的对话式诊断先经过Phase 0.5和Phase 2的真实验证，确认方法本身可靠之后，才重新讨论是否扩展到第二个领域（且第二个领域也应该优先选择另一个反馈信号相对清晰的技能领域，而非直接跳到"通用认知"）。

---

## 2. 双Agent互校机制的重新引入（对话诊断场景下的新设计）

**想法**：Yang et al. (2024b) 提出的三角色多智能体知识追踪设计（administrator/judger/critic）与ECOS的dual_agent思路结构相似，但应用场景不同（对话诊断 vs 做题判分），可能是重新尝试双Agent机制的一个不同切入点。

**为什么现在不做**：ECOS的H3假设（双Agent互校降低幻觉）在做题判分场景下统计上显著反向，尚未有证据支持换个场景就会成立。属于GOVERNANCE规则2（"验证通过"需要独立方法交叉印证）覆盖范围，不该在没有新证据前默认复用。

**触发条件**：Phase 0.5 spike若发现单一LLM的对话诊断存在系统性、可复现的偏差模式（而不是随机噪声），且这个偏差模式看起来适合用"多角色互相校验"来纠正，再考虑参照这篇文献重新设计一版，而不是直接照搬ECOS的dual_agent代码。

---

## 3. 插件化/API产品化

（沿用自MIGRATION.md的既有决定）触发条件：Phase 2验证出正向信号，且用户明确反馈"想要更开放的对话式学习"或"想要把这个能力接入到其他工具里"。

---

## 4. per-skill 校准曲线（全局曲线先行）

**想法**：A1 自评置信度校准曲线当前按全局单表（CogMirror 的 domain = 全局）；未来可按 skill 各建一张曲线，让校准粒度更细。

**为什么现在不做**：单用户数据少，分 topic 后桶更稀疏、多数桶样本不足只能走回退路径；先让全局曲线在 P2 验证价值。

**触发条件**：单用户 response ≥ 300 且分 topic 桶样本 ≥ 20。

---

## 5. 对话式 LLM 进产品判分链路

**想法**：把 Phase 0.5 spike 的 LLM 对话式诊断从技术预研推进到 P0 判分链路。

**为什么现在不做**：P0 判分以确定性静态题库为核心（判分链路无 LLM 是硬规则）；spike 结论上限是自测 n=1「初步观察」，不构成「验证通过」，不满足进产品的证据门槛。

**触发条件**：先有 P2 校准 + P1 回归作安全轨；且 Phase 0.5 spike 多人真人验证通过。

---

## 6. 独立 session_summaries 表

**想法**：B1 纵向档案/会话聚合若命中性能瓶颈，可建独立会话摘要表做缓存。

**为什么现在不做**：单用户 n 小，从 `responses`/`belief_snapshots` 实时聚合成本可忽略；硬搬独立表反而是过度设计（理由同 P2 不建独立校准表）。

**触发条件**：聚合查询出现可测的性能问题（当前规模不会）。

---

## 7. Telegram/每日一题分发

**想法**：借 PersonalAGI 的调度/campaign 模式做「每日一题」等主动触达分发（如 Telegram 机器人）。

**为什么现在不做**：CogMirror 是本地同步 CLI，A4 间隔衰减已退化为会话开始时的一次同步计算，不需要 APScheduler/CronTrigger/Telegram 基建；分发属产品形态扩张。

**触发条件**：Phase 2 正向信号 + 用户明确要求。

---

## 8. 向量记忆 / Qdrant

**想法**：借 PersonalAGI memory 的 4 层记忆引入向量检索做跨会话主动召回。

**为什么现在不做**：单用户跨会话召回用 SQLite 聚合即可（B1 已落地为确定性启发式），不需要向量基建与新依赖。

**触发条件**：学习者数量进入多用户阶段。
