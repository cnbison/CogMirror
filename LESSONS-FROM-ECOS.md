# LESSONS-FROM-ECOS：从 ECOS 项目带来的教训

> **性质**：ECOS 项目（K12"教育认知操作系统"，2026年6月-8月，v0.1-v0.96）两个多月真金白银换来的教训浓缩。本文档是团队（哪怕只有一人+AI 协作）的共同记忆，写下来是为了不在新项目里重蹈，不是为了怀旧。
> **配套**：硬规则版本见 [GOVERNANCE.md](./GOVERNANCE.md)（本文是背景和"为什么"，那边是可执行的"必须怎么做"）。

---

## 1. "验证通过"必须有至少两种独立指标交叉印证

**发生了什么**：ECOS 的 H3 假设（双 Agent 互校降低幻觉）两次被发现是指标选错导致的假阳性；"0 拐点"的完美结论后来被证实是三个 bug 叠加的假象，而不是真的表现完美。H3 最终验证结果：统计上显著**反向**（p<0.0001）。

**新项目的应用**：
- 任何"XX 有效"的结论，发布前先问：换一种测量方式，结论还成立吗？
- 单一指标跑通只能标注为"初步观察，待复核"，不能标"验证通过"
- 这条已经写进 GOVERNANCE.md 规则2，不要因为赶进度而绕过

## 2. 测试者不能是开发者本人

**发生了什么**：ECOS 阶段"3 个测试用户"里的第一个（lbc001）就是开发者自己，这一点直到复盘阶段才被明确指出，严重削弱了"真实用户验证"的含金量。

**新项目的应用**：
- Phase 2 的正式验证样本（5-10 人）必须是真实的、非团队成员的成年 Python 自学者，不能是"捧场式"的朋友圈测试
- 开发者自测数据要明确标注"自测数据，不计入正式验证样本"（ROADMAP Phase 1 已固化此原则）
- 已写进 GOVERNANCE.md 规则3

## 3. 抽象化工作要有日历硬约束，"知道问题"不等于"会自动避免"

**发生了什么**：ECOS 在 6月27日 就自我诊断出"框架先于应用"是最大风险，但 8月11-17日（v0.83-v0.94）仍然花了一整周做 Kernel 通用化。诊断书写的存在并没有阻止复发——**需要外部约束，而不是依赖自觉**。

**新项目的应用**：
- 任何"要不要把 XX 做成可插拔通用能力"的冲动，默认延后到：至少完成一轮真实用户验证，或出现第二个真实复用场景
- 冲动出现时写进 `SOMEDAY.md`，然后继续手头的具体验证工作
- 已写进 GOVERNANCE.md 规则4；MIGRATION.md 第0节的"多搬了什么"是同一教训在迁移场景下的变体

## 4. 版本号/完成度标注要对应真实验证程度，不是工程完成度

**发生了什么**：ECOS 的 LearningDNA 功能曾出现"标 100% 完成，但核心指标从未真正非零"的情况，累计 4 次虚标。一周内跳多个小版本、大量"✅"堆积，是"工程产出量"和"科学验证进度"两条线脱节的典型信号。

**新项目的应用**：
- 版本号从 v0.1.0 重新开始（不继承 ECOS 的 v0.9x，那个数字会错误暗示成熟度）
- 汇报进度时永远分两条线："工程完成了什么"和"验证证实了什么"，不合并
- "完成"的标准：如果现在有人要一个可复现的演示，能不能立刻做到？做不到就不是完成
- 已写进 GOVERNANCE.md 规则1、规则5

## 附：一条补充教训（数据事故）

ECOS 在 2026年8月19日 发现一个持久化 bug，导致某测试用户三周真实数据丢失/错误写入，且恰好覆盖多轮算法验证周期。修复 bug 之后，"哪些既有结论被这批数据污染"没有被同步回答。

**新项目的应用**：任何数据事故（丢失、错写、串号）修复后，必须追加一步——列出所有依赖受影响时间窗口数据的既有结论，逐条标注是否需要重新验证（GOVERNANCE.md 规则6）。

---

## 本次迁移（2026-08-22）对上述教训的即时应用

- dual_agent 模块**不迁移**：H3 未验证成立的核心机制不能成为新产品地基（教训1的直接应用）
- POMDP+PBVI 不迁移：数据规模不支撑的重型方法不进 MVP（"验证优先于抽象"）
- 5D 状态更新的数值合理性（非全零、非全同）作为 Phase 0 关卡的自动化测试存在，而不是靠肉眼看看就算通过
- 本项目第 0 天就带着这套教训文档和 GOVERNANCE 硬规则开工，而不是等踩坑后再补
