# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与[语义化版本](https://semver.org/lang/zh-CN/)。
版本号自 v0.1.0 重新开始（不继承 ECOS 的 v0.9x）。条目用中文，风格与 git 提交信息一致（范围前缀 + 描述）。

## [Unreleased]

### 新增

- 伪自信逐题即时提示：自评高但实际得分低（命中伪自信）时当题点出「伪自信提示：你自评 X%，但这题实际得分 Y%——落差有点大，『感觉会』可能掩盖了『其实还没会』，地图会把这个点标出来」；未命中静默。至此认知地图三核心元素（伪自信/临界概念/一句话建议）全部从"地图标注"变成"做题当下的实时信号"
- 练习轮逐题临界概念反馈：做题当下就把临界概念变成可感知的进度——L3+ 答对报「「循环」跨越进度 N%——再答对 M 次 L3+ 题即跨越」（剩余次数随连续答对递减）、liminal 中答错报「这次答错，跨越进度回落…这不是退步，重来一组即可」、跨过瞬间报「已跨越！恭喜」；pre_liminal / 非 L3+ 答对静默不刷屏。临界概念从"地图上的标注"变成"做题当下的实时进度"
- 认知地图新增「整体解读段」：地图标题下方先给一段「你现在处于什么状态、为什么、下一步」的综述（先结论后证据，服务"无开发者解释可懂"）；综合作答分布定整体基调（得分率→良好/中间/薄弱），MIRT 生效后才点维度差（K 高 P 低=「知道概念、转代码还在建立」，反之「程序技能更扎实」——且维度结论只在"确实掌握"水平才下，避免全错学习者被误读），伪自信/liminal 子句各自回指但换角度不重复分节文案，样本 <5 时如实说明信号不稳定
- 题库 L3+ 扩充（F10 收口）：5 个 topic 各补到 6 道 L3+（新增 18 题，题库 20→41），临界概念三态（pre→liminal→post_liminal）产品内端到端可达；新增 code 题含全局计数（ps-l3-02，TestCases 跨用例共享 namespace）
- 认知地图新增「已跨越」确认文案：post_liminal 态的临界概念以「已跨越（恭喜，这些概念你已经真正掌握）」列出
- CLI 新增 `--topic` / `--level` 筛选（接受 L3 或 APPLY 等别名），地图「一句话建议」下输出「立刻练习」可执行命令——让「建议做 3 道某 topic 的 L3 题」真正可执行
- 交互式「按建议直达练习」：认知地图末尾直接询问是否按建议练习（[y/N]），接受则进入目标题组的练习轮并重渲染地图，直至临界概念跨过或用户拒绝——无需再手动复制命令重跑
- 建议带「为什么 + 可行动进度」：临界概念建议说明「正在跨越中——再答对 N 次 L3+ 题即跨越」（N 从 liminal 连续答对计数推导，阈值单一来源 = detector）；最弱 topic 建议标注「是当前最弱的一项」；认知地图 liminal 行与练习交互提示同步露出剩余次数——把"跨越进度 90%"翻译成下一步能做的动作

### 修复

- 代码判分递归误判：`exec` 拆开 globals/locals 导致用户函数的 `__globals__` 落在独立 dict，正确的递归代码被判 `NameError`（自测发现）；改为同一 dict，`make_counter` 专用判分同修
- C 维度伪自信折扣持久化到 discount_factor：此前命中伪自信后数值会被下一次 MIRT 重算覆盖，现在每次更新末尾按 `sigmoid(θ_C) × discount_factor` 折算；顺带修复 misconception 折扣双重应用的问题
- 认知地图诚实标注：X 维度标注「MVP 未提供支架/提示机制，暂未测量」，Bloom L5/L6 标注「暂无对应层级题目」（题库当前最高 L4），不再显示先验假数值
- C 维度改为状态显示：无失准证据时不再显示误导性先验数值（0.55 曾被读成"中等自信"），显示「未发现失准（N 次自评与表现一致）」或「暂无自评数据」；有伪自信命中才显示打折后的数值
- Bloom 六层步长 0.05 → 0.12：练过的层与未练的层拉开可见差距（自测反馈"六层数值差异不大"）
- 自评 prompt 末尾加换行：跳过自评（直接回车）时下一提示不再粘连（真机自测复现）
- map-only 恢复时保留 self_confidence，C 维度状态在二次进入时不再误判"暂无自评数据"
- TC 显示名统一为单一来源 `engine.tc_detector.tc_library`，删除 content 库的隐式 key 拼装，同步两库文案避免漂移
- 修正 liminal → post_liminal 判定：需连续 3 次 L3+ 正确才跨越（此前规则未真正生效）
- Bloom 主导层级只从有观测的层里选：`covered_layers` 随作答累积并参与序列化，未练层不再以先验 0.5 反超——弱学习者不再被误判「主导 EVALUATE」，无作答数据时显示「暂未测量」
- 伪自信点全量列出：去掉 `[-5:]` 截断，列表条数与 C 维度「发现 N 处失准」一致（此前计数 8 但只列 5）
- 一句话建议的 topic 用中文短名（变量赋值/循环/函数/递归/作用域），不再露出原始英文 id（如 python.recursion）

### 说明

- 临界概念三态机经双线验证（detector 单测 + 引擎集成测试）：liminal 进入、答错重置连续计数、3 次连续 L3+ 正确后 post_liminal 均按规则生效。已知限制：题库各 topic 的 L3+ 题量不足（仅 loops 有 3 道），post_liminal 态在产品内端到端暂不可达，已记 SOMEDAY
- 产品定位调整（2026-08-23）：对话式认知诊断上调为核心差异化能力（PRD 第 8/8a/8b 节），ROADMAP 新增 Phase 0.5 技术 spike（插入 Phase 0 之后）——验证 LLM 从开放对话给出能力估计与独立 ground truth 是否稳定相关；技术方案采用锚定 Bloom+SOLO+5D 图谱的动态追问式对话，P 维度必须提交可执行代码锚点；新增 [REFERENCES-dialogue-assessment.md](./REFERENCES-dialogue-assessment.md) 文献整理
- SOMEDAY.md 重写为触发式清单（跨领域诊断 / 双Agent互校重新引入 / 插件化），均注明触发条件，未满足前不重新讨论
- 5D 语义分层决策（2026-08-24）：C/X 定义差异（PRD 概念联结/元认知 vs 代码现状 置信度/外部支架）根因是潜变量与观测指标同名混用（构念污染）。定稿：5D 语义基准 = C 概念联结、X 元认知（L1 潜变量），置信度/外部支架降级为 L3 观测证据变量（不删除，参与 L1 推断）；状态模型显式三层分层（L1 潜变量 / L2 结构化投影 Bloom·SOLO·TC / L3 观测证据）。可测性排序：C=概念联结 的观测证据静态题库采不到、只有对话模式（Phase 0.5）能采到 → 语义基准现在定，但代码不重命名、C 在 Phase 0.5 前保持未测量，重写 C/X 推断须等 spike 数据。讨论记录见 [docs/c-x-gpt.md](./docs/c-x-gpt.md)，落点见 [PRD.md](./PRD.md) 8b 与 [GOVERNANCE.md](./GOVERNANCE.md) 规则7
- Phase 0.5 对话诊断 spike 收口（2026-08-26）：工具链完成（`spike/` 目录，185 测试全绿，MiniMax-M3）；真实 n=1 三轮（dev-1/dev-1-r2/dev-1-r3）方向一致——对话估计 ≈ 题库锚点或略低、无系统性高估（dev-1-r3 Δ=-0.03 最干净）；决策不做 5-8 人真人轮（自测驱动、产品未成熟），spike 结论上限=初步观察/自测数据，**P0 判分仍以确定性静态题库为核心、对话式诊断作增强/技术预研**；dev-1-r4 暂缓；5D 区分度验证随多人验证推迟。详见 [ROADMAP.md](./ROADMAP.md) Phase 0.5「执行情况」与 [PRD.md](./PRD.md) 8a
- F10 收口验证（2026-08-26，自测数据，不计入正式验证样本）：`--topic python.loops --level L3 --questions 3` 全对 → liminal 90%；再 3 题全对 → 「已跨越 循环」；`--map-only` 恢复持久；空筛选提示友好。双指标：pytest 203 全绿 + 真机 CLI
- 交互式「按建议直达练习」验证（2026-08-27，自测数据，不计入正式验证样本）：主会话 3 道 loops-L3 全对 → liminal 90% + 交互提示「按建议现在练 3 道「循环」的 L3 题吗？」；答 y → 练习轮 3 题全对 → 「已跨越 循环」且不再重复提示（已跨越不再建议）；答 n → 只渲染一次地图即退出。双指标：pytest 205 全绿 + 真机 CLI
- 建议进度文案验证（2026-08-27，自测数据，不计入正式验证样本）：主轮 3 道 loops-L3 全对 → 地图 liminal 行「循环是受控的重复（跨越进度 90%，再答对 3 次 L3+ 题即跨越）」、建议「「循环」正在跨越中——再答对 3 次 L3+ 题即跨越」、练习提示「（再答对 3 次 L3+ 题即跨越）[y/N]」；答 y 练习轮 3 对 → 「已跨越」。另一用户全答错 → 建议「「变量赋值」是当前最弱的一项（掌握概率 11%）」，练习提示无剩余次数（非 liminal 目标不加）。双指标：pytest 208 全绿 + 真机 CLI
- 整体解读段验证（2026-08-27，自测数据，不计入正式验证样本）：①liminal 分支——3 道 loops-L3 全对 → 「你目前整体掌握良好：3 道题里 3 道完整答对。…「循环是受控的重复」正在跨越中，这是学习的正常中间态，不是退步。作答样本还少（3 题）…」；②薄弱+伪自信分支——variables 5 题全错（自评 100）→ 「整体还比较薄弱…5 处伪自信…后者更值得放慢检查」，无「程序技能更扎实」误读；③无历史 map-only → 「还没有作答记录…（当前显示的是先验默认值）」。双指标：pytest 215 全绿 + 真机 CLI 三分支
- 逐题临界概念反馈验证（2026-08-27，自测数据，不计入正式验证样本）：主轮 3 道 loops-L3 全对 → 第 3 题逐题行「「循环是受控的重复」跨越进度 90%——再答对 3 次 L3+ 题即跨越」（前两题静默）；接受练习再 3 对 → 逐题行「跨越进度 100%——再答对 2 次」→「再答对 1 次」→「已跨越！恭喜」；另一用户 liminal 中答错 → 「这次答错，跨越进度回落到 75%——这不是退步，重来一组即可」。双指标：pytest 220 全绿 + 真机两分支
- 伪自信逐题提示验证（2026-08-27，自测数据，不计入正式验证样本）：自评 100 + 答错 → 当题点出「伪自信提示：你自评 100%，但这题实际得分 0%——落差有点大…地图会把这个点标出来」，地图 C 维度「发现 2 处失准」汇总一致；高自评 + 全对 → 无提示行、地图「未发现失准（4 次自评与表现一致）」。双指标：pytest 222 全绿 + 真机命中/未命中两分支

## [v0.1.0] - 2026-08-22

Phase 0 从 ECOS 选择性迁移并搭建的最小可运行链路。

### 新增

- CLI 最小链路：做题 → 5D 状态更新 → 认知地图展示（`python -m cogmirror.cli`）
- 静态 Python 题库（5 个 topic × Bloom L1-L4）+ 确定性 partial credit 判分（choice/fill/code，本地跑测试用例）
- 5D 信念引擎：MIRT 载荷估计 + BKT 掌握概率 + Bloom 六层 + TC 三态机（pre/liminal/post）+ 伪自信检测
- SQLite 持久层：用户 / 作答记录 / 状态快照，支持数据导出与删除（成人向合规，无监护人字段）
- 自动化测试：pytest 全绿

### 说明

- 不迁移 ECOS 的 dual_agent 互校、POMDP 策略、Multi-Domain 内核（见 `LESSONS-FROM-ECOS.md`）
- MVP 无 LLM 依赖：静态题库 + 确定性判分为用户确认的方案
